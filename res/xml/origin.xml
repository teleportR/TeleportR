<?xml version="1.0" encoding="utf-8"?>

<searchable xmlns:android="http://schemas.android.com/apk/res/android"
        android:searchButtonText="Hui"
        android:hint="@string/origin_hint"
		android:label="@string/search_label"
        android:searchSuggestThreshold="0"
        android:queryAfterZeroResults="true"
        android:searchSuggestPath="origin"
        android:searchMode="queryRewriteFromText"
        android:searchSuggestAuthority="de.andlabs.teleporter"
        android:searchSuggestIntentAction="org.teleportr.BEAM"
        >
</searchable>
